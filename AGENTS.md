# 简播项目执行约束

1. 软件名固定为“简播”，仓库名固定为 `simple_video`。
2. 密钥、密码、`keystore.properties`、签名证书不得提交到公开仓库。
3. 删除分组只能删除应用内索引/隐藏记录，不得调用本地文件删除接口。
4. 常规自动扫描使用 MediaStore；手动文件夹导入使用 SAF，并保留用户授予的读取权限。针对未进入 MediaStore 的微信隐藏视频，只允许在用户显式授予 `MANAGE_EXTERNAL_STORAGE` 后，用 File API 深度扫描共享存储中的已知微信目录（`Android/media/com.tencent.mm`、`Tencent/MicroMsg`），不得借此扫描其他应用私有目录，也不得尝试读取 `Android/data`。
5. 播放内核优先使用 AndroidX Media3，不自行维护解码器。
6. 单个构建、扫描、测试或迁移步骤运行满 5 分钟时，必须立即检查进程、日志和资源占用，确认不是卡死后再继续；不得无反馈长时间等待。
7. 每次播放器手势或方向逻辑修改后，至少验证：竖屏视频、横屏视频、反向横屏、上一项/下一项、倍速、亮度、音量、暂停、自动隐藏控制栏。
8. 微信隐藏视频扫描不得设置视频文件大小上限；已知视频扩展名不按大小过滤。对无扩展名或临时扩展名文件，只允许用轻量文件头识别补充发现视频容器，避免对大量无关小文件逐个做重型媒体解析。
