# 简播 1.0.11

- 支持从微信等应用的“更多打开方式”直接选择简播播放视频，兼容 `ACTION_VIEW` 与 `ACTION_SEND`，并兼容常见 `video/*` 及 `application/octet-stream` 文件类型。
- 外部视频播放结束后可选择“授权所在文件夹”。Android 不允许应用凭单个文件静默取得整个父目录权限，因此由系统文件夹选择器确认一次；确认后使用 SAF 持久保存读取权限，并将同目录视频加入简播。
- 当外部文件来自 Android DocumentsProvider 且能解析真实父目录时，系统文件夹选择器会尽量预定位到该目录；微信只给临时 `content://` URI 时无法伪造父路径，需要用户在选择器中确认实际目录。
- 文件夹详情页在“X 个视频”后显示可获得的文件夹位置，例如 `内部存储/DCIM/Camera`、`内部存储/Android/media/...`。
- 修复 1.0.10 “微信隐藏视频扫描”可能把已由 MediaStore 找到的视频再次显示的问题：现在为 MediaStore、SAF 和直接文件扫描统一保存稳定存储位置，并按实际位置去重。
- 保持文件夹图标使用主题主色，不恢复黑色文件夹图标。
- 版本升级为 `1.0.11` / `versionCode=12`，applicationId 保持 `com.luxiaoshi.jianbo`，继续使用原简播正式签名以支持覆盖升级。
