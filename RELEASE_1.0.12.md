# 简播 1.0.12

- 修复 1.0.11 外部视频授权父文件夹时无条件落到“内部存储”根目录、导致“无法使用此文件夹”的问题。
- “加入这个视频所在文件夹？”弹框固定显示真实“文件位置”和“所在文件夹”。
- 外部 URI 真实位置依次尝试 DocumentsProvider、MediaStore 相对路径、Provider `_data` 和 `/proc/self/fd`；无法取得物理路径时明确显示原始 `content://` URI，不猜测路径。
- 能解析共享存储父目录时，系统文件夹选择器尽量直接定位到该具体目录，而不是存储根目录。
- 若真实路径属于微信/其他应用私有存储，明确说明 Android 不能授权整个父目录，避免继续弹出无意义的 SAF 授权。
- 版本：1.0.12 / versionCode 13；包名保持 `com.luxiaoshi.jianbo`。
